# Works Log — Problems & Precise Solutions

A concise summary of engineering problems encountered and their solutions on the STM32 Agricultural Rover platform.

---

| # | Problem | Root Cause | Precise Solution |
|---|---|---|---|
| 1 | Linker failure on `.ARM.extab` | `(READONLY)` keyword in CubeMX linker script incompatible with GCC 10 | Removed `(READONLY)` from all section definitions in `STM32F411CEUX_FLASH.ld`. |
| 2 | Compile error in `steer_pid.c` | `LOOP_HZ` referenced without including `config.h` | Added `#include "config.h"` in `steer_pid.c`. |
| 3 | Kinematic radius error on sharp turns | `sinf(delta)` used instead of `tanf(delta)` in `Ackermann_ComputeRPM` | Changed turning radius formula to $R_{\text{rear}} = \frac{W_b}{\tan(\delta)}$ in `ackermann.c`. |
| 4 | Steering stalls on transient ADC glitch | `out->valid` set to 0 on single missed read, bypassing 5-tick debounce | Held previous angle and kept `valid=1` until `fault_runs >= STEER_FAULT_THRESHOLD` in `ads1115.c`. |
| 5 | Reverse differential ratio compression | Outer wheel clamped independently in `_ToThrottle` at 10 RPM | Scaled both wheel RPMs down proportionally inside `Ackermann_ComputeRPM` for reverse mode. |
| 6 | Derivative kick on rapid stick movement | PID differentiated on error $(target - actual)$ | Switched PID derivative calculation to derivative-on-measurement in `steer_pid.c`. |
| 7 | Actuator PWM capped at 99.9% | Compare register clamped to ARR (999) instead of ARR+1 (1000) | Allowed `CCR` to reach `ACT_PWM_ARR + 1` at 100% duty cycle in `actuator.c`. |
| 8 | Reverse max speed decoupled from RPM cap | Hardcoded float literal `0.183f` used in `ackermann.c` | Derived `VMAX_REV_MS` directly from `THR_REV_RPM_CAP` and `WHEEL_RADIUS_M`. |
| 9 | Standstill pivot inner wheel rotation | Differential drove both wheels in pure Ackermann mode | Configured single-wheel pivot in `ackermann.c`: inner rear wheel held at 0 RPM, outer wheel crawls forward at ~7.5 RPM after 80% steering lock. |
| 10 | Steering pot raw min/max were placeholders (5000/25000) | Never calibrated on hardware | Measured lock-to-lock raw ADC values with a standalone ESP32 + ADS1115 rig (gain/wiring matched to STM32 firmware) and entered into `ackermann_config.h`: L 6813–15766, R 6059–14279. |
| 11 | Actuator likely to chatter at tight ±0.5° deadband | Actuator is 100%-speed bang-bang with no low-speed settle mode | Widened `STEER_DEADBAND_DEG` from 0.5° to 1.3° in `ackermann_config.h`. |
| 12 | Actuator hunting/oscillating trying to settle at zero on hardware | Confirmed on hardware: ±1.3° deadband too tight for the actuator's overshoot at 100%-speed bang-bang drive | Widened `STEER_DEADBAND_DEG` further, 1.3° → 2.5°, in `ackermann_config.h`. |
| 13 | Build failed: `ACK_STRAIGHT_DEG`, `STEER_INTEGRAL_MAX_PCT`, `KP/KI/KD_STEER` undeclared | Dropped during Phase 2 port from `STM_ackermann_backup/Inc/ackermann_config.h` into `Rover_closed_loop/Core/Inc/ackermann_config.h` | Re-added all four constants with their original backup values; project now builds 0 errors. |
| 14 | Wheels settle offset to the right at commanded zero | 2-point (min/max only) raw->angle calibration assumes both mechanical locks sit exactly at ±45° from true straight; measured raw at true straight (L=13098, R=10315) showed `angle_L`=+18.18°, `angle_R`=+1.60° instead of 0° each — a 16.6° L/R disagreement, not just an averaged offset | Added per-wheel `ANGLE_TRIM_L_DEG`/`ANGLE_TRIM_R_DEG` (18.18 / 1.60), subtracted in `_MapToAngle()` in `ads1115.c` — both wheels now independently read ~0° at true straight, not just their average. |
| 15 | Standstill pivot only crawled the outer wheel at a fixed ~7.5 RPM derived from `PIVOT_CRAWL_MS`, inner wheel fully locked at 0 RPM | Design choice from initial port | Changed to `PIVOT_OUTER_RPM`=10.0, `PIVOT_INNER_RPM`=6.0 (both wheels now roll forward — no longer a true stationary-inner pivot, a tight crawl-turn instead) in `ackermann_config.h`/`ackermann.c`. Gate on `PIVOT_LOCK_FRACTION` (80% lock before differential applies) preserved unchanged. |
| 16 | Holding steering stick at ~25-30% with no throttle snapped the wheels to full lock instead of holding ~25-30% of max angle | Standstill branch of `Ackermann_Run()` hardcoded `target_steer_deg = ±MAX_STEER_ANGLE_DEG` (snap to full lock) any time `\|Xn\| > ACK_STICK_DEADBAND` (5%) while throttle was neutral — it never used the proportional `-Xn * MAX_STEER_ANGLE_DEG` formula that the throttle-applied branch uses | Changed the standstill branch to the same proportional formula. Pivot-crawl drive still only activates once measured angle reaches 80% of lock (`PIVOT_LOCK_FRACTION`), which now only happens if the stick is genuinely held near its own extreme — moderate stick deflection just steers and holds, no drive. |
| 17 | Left potentiometer replaced — old raw calibration and zero trim invalid | Physical hardware change, not a software bug | Re-swept lock-to-lock + straight-ahead with the ESP32 rig: `ADC_L_MIN/MAX_RAW` 1920/4107, `ADC_R_MIN/MAX_RAW` 5722/12500 (right also shifted, though only the left pot was reportedly replaced — possibly disturbed during the swap). ⚠️ new left pot's span (2187) is 3.1x narrower than right's (6778) — flagged for hardware follow-up, not yet resolved. |
| 18 | Left pivot turn never achieved, even at full left lock (right pivot worked fine) | The flat-offset trim (`ANGLE_TRIM_L/R_DEG`, item 17) fixed the zero-point but was applied *before* clamping to +-45°, which silently compressed the left side's usable range: at true full left lock, computed `delta` only reached ~29.1° — short of the 36° (80% lock) needed to trigger the pivot. Right side happened to saturate at the -45° clamp early instead, masking the same underlying flaw. | Replaced the flat-trim design with a proper 3-point (piecewise-linear) calibration: `ADC_L/R_CENTER_RAW` (3500/10000) added: min->center maps to -45..0, center->max maps to 0..+45, independently per wheel. `ANGLE_TRIM_L/R_DEG` removed. Verified analytically: straight now reads exactly 0.0/0.0, full left lock reads exactly +45/+45 (delta=45, was 29.09), full right lock exactly -45/-45. Not yet re-tested on hardware. |
| 19 | Rover consistently drifts left when commanding straight, requiring manual right correction on the stick | `ACK_STRAIGHT_DEG` (1.0°, differential's "treat as straight" threshold) was narrower than `STEER_DEADBAND_DEG` (2.5°, actuator's "close enough" threshold). The actuator could legitimately rest anywhere within +-2.5° of commanded straight; any residual angle between 1.0° and 2.5° still made `Ackermann_ComputeRPM()` apply a real L/R speed differential, steering the rover toward whichever side the actuator happened to settle on. | Raised `ACK_STRAIGHT_DEG` from 1.0° to 3.0° (>= `STEER_DEADBAND_DEG`) in `ackermann_config.h`, so nothing inside the actuator's acceptable resting zone can trigger a differential. Not yet re-tested on hardware; if drift persists, next suspect is rear motor/DAC asymmetry rather than steering. |
| 20 | Needed a MANUAL/AUTO command-source select for the planned RPi autonomous link, but no free 2-position switch existed on the transmitter | `SWB` was already assigned to the reverse-speed-cap function (2-position, 60%/100%) | Repurposed `SWB` as MANUAL/AUTO select, implemented in `ibus.c`/`main.c` — see row 22; reverse-speed-cap feature dropped as a consequence (`rev_speed_pct` now fixed at `1.0f`). |
| 21 | `main.c` would fail to compile — used `IBUSData_t`, `Ackermann_Run()`, `Motor_Init()`, etc. with no `#include` for any of `ibus.h`/`ackermann.h`/`motor.h`/`encoder.h`/`ads1115.h`/`actuator.h`/`steer_pid.h`/`ackermann_config.h` anywhere in the file | A prior edit had added these includes *above* `main.c`'s `USER CODE BEGIN/END Includes` markers instead of inside them; today's CubeMX "Generate Code" (for the USART2/RPi-link peripheral) regenerated everything outside USER CODE markers from scratch, silently deleting them — exactly the regeneration risk `Rover_study.md` §10.3 warns about | Re-added all eight includes (plus `rpi_link.h`, `<math.h>`, `<stdio.h>`) inside the `USER CODE BEGIN Includes`/`END Includes` block in `main.c`, so they survive future regenerations. Not yet rebuilt (no ARM toolchain available in this environment) — needs a clean STM32CubeIDE build to confirm. |
| 22 | Needed the STM32 side of the RPi autonomous link: USART2 framing, MANUAL/AUTO arbitration, link-loss failsafe | New capability (Phase 5), not a bug | Added `rpi_link.c/h` (DMA+IDLE binary framing on USART2, mirrors `ibus.c`'s pattern for USART1); added `Ackermann_RunAuto()` to `ackermann.c/h` (bypasses joystick deadband/curve, takes RPi's absolute angle/speed targets directly); wired `main.c` to branch on `rc.auto_mode` and force `ACK_STATE_BRAKE` when AUTO is selected but `RPiCmd_t.link_ok` is false (implements the 300 ms `AUTO_UART_TIMEOUT_US` failsafe). Feedback frame (measured angle + wheel RPM + status) sent to the RPi every 20 Hz tick regardless of mode. Firmware built and flashed to the STM32; RPi side of the link not yet exercised. |
| 23 | Needed IMU heading/attitude data on the RPi5 for the autonomous guidance stack | New capability (Phase 5), not a bug | Added `RPi_companion/imu.py` (BNO055 driver via `board.I2C()` + `adafruit_bno055`, same library family as the old BBB stack, now Pi5-compatible via Blinka), `test_imu.py` (live readout sanity check), `calibrate_imu.py` (full 4-axis calibration ritual + offset dump). Old BBB calibration offsets intentionally not reused (different sensor/mounting). Software stack now deployed and verified on the actual RPi5 (see row 24-26) — only the physical sensor reconnection remains. |
| 24 | RPi5's header I2C bus (GPIO2/GPIO3) wasn't available | `dtparam=i2c_arm=on` was commented out in `/boot/firmware/config.txt` by default on this Debian 13 "trixie" image; two unrelated RP1-internal I2C buses (`i2c-13`,`i2c-14`) existed and could be mistaken for it | Uncommented `dtparam=i2c_arm=on`, rebooted. Confirmed via `i2cdetect -l`: `i2c-1` (DesignWare adapter) is the correct header bus. |
| 25 | GPIO14/GPIO15 (the STM32 UART link pins) were occupied by the Linux login console | `console=serial0,115200` in `cmdline.txt` + an auto-generated `serial-getty@ttyAMA10.service` (Pi5 numbers this UART `ttyAMA10`, not `ttyAMA0`) | Removed `console=serial0,115200` from `/boot/firmware/cmdline.txt` and disabled the getty unit — confirmed both `inactive`/`disabled` and non-regenerating after a reboot (removing the kernel parameter is what makes it permanent, not the `disable` alone). |
| 26 | `pip install -r RPi_companion/requirements.txt` failed twice building the `lgpio` wheel | Missing system (non-Python) build dependencies: first no `swig` binary (can't generate the C wrapper), then `liblgpio-dev` missing (runtime `.so` present but not the dev symlink `ld` needs for `-llgpio`) | Installed `swig` and `liblgpio-dev` via `apt-get`; install then succeeded. Documented both as prerequisites in `requirements.txt`'s header comment so a future setup doesn't rediscover this by trial. |
| 27 | `check_stm32_link.py` reported zero feedback frames received with the STM32+RPi UART physically connected | **Two stacked RPi-side causes, both confirmed via `dmesg`/`pinctrl`:** (1) onboard Bluetooth silently attached to the same UART as GPIO14/15 (`hci_uart_bcm`), leaving it at 9600 baud with the wrong line discipline; (2) the real root cause — `enable_uart=1` was never set in `config.txt`, so GPIO14/15 were never actually muxed to UART function at the hardware level (`pinctrl get 14,15` showed `none`/`none`), even though `/dev/ttyAMA10` existed as a device node and a getty had run on it previously — neither of those actually proves the pins are hardware-muxed | Added `dtoverlay=disable-bt` (disables Bluetooth on this Pi permanently) and `enable_uart=1` to `/boot/firmware/config.txt`, rebooted. Verified via `pinctrl get 14,15` → `TXD0`/`RXD0`, then via raw capture and both `check_stm32_link.py` (20.0 frames/s, 0 bad) and `read_encoders.py` (sane live data). **Link fully working end-to-end**, confirmed with the STM32 firmware unmodified — the STM32 side was correct all along (ESP32 sniffer proved this before the fix). |
| 28 | `test_imu.py` printed "board/adafruit_bno055 not available... expected off the actual Raspberry Pi" while running directly ON the Pi | User ran `python3 test_imu.py` (system Python) and `sudo python3 test_imu.py` — neither sees the packages, which only exist in `~/rover-venv` (`sudo` uses root's own `python3`, a different interpreter entirely, not the venv). The message wording didn't distinguish "wrong Python env" from "not a Pi at all", so it looked like a hardware/driver problem when it was just a missing `source ~/rover-venv/bin/activate` | Fixed the message in `imu.py` to name the actual cause and the fix (`ImportError` → venv hint; `NotImplementedError` → genuine non-Pi host, kept separate). No driver was ever needed — BNO055 does its own on-chip sensor fusion over plain I2C, already proven working. |
| 29 | `git push` failed with "could not read Username for 'https://github.com': No such device or address" right after `gh auth login` succeeded | `gh` storing a valid token in its own keyring doesn't automatically configure it as git's credential helper — a separate step | Ran `gh auth setup-git` once; `git push` worked immediately after. |
| 30 | Needed a waypoint-following guidance stack for the RPi (no RTK — encoder+IMU dead reckoning only) | New capability (Phase 5), not a bug | Studied the BBB-era dead-reckoning scripts in `Old_files/dev_bak/` first; discussed algorithm/parameters with the user before building. Added `rover_config.py` (shared tunables), `waypoints.py` (straight/rectangle/circle/lawnmower generators + CSV I/O), `odometry.py` (dead-reckoning pose), `guidance.py` (Pure Pursuit — chosen over the old heading-error-PID approach since it outputs a steering angle directly, matching Ackermann). Found and flagged a real physical-feasibility issue: 1.0m lawnmower row spacing implies a tighter U-turn radius than this chassis can steer; `waypoints.py` clamps to the achievable radius and warns rather than generating an undrivable path. All three logic modules verified via standalone smoke tests; not yet deployed to the RPi (network unreachable at write time) or wired into an actual running mission loop. |
| 31 | Both steering potentiometers replaced — old raw ADC calibration invalid | Physical hardware change, not a software bug | Updated `ADC_L/R_MIN/MAX/CENTER_RAW` in `ackermann_config.h` to user-supplied values: L 3096/4426/5793, R 2124/3167/4160. Spans well-balanced this time (~3-5% split each side, vs. the previous replacement's flagged 3.1x mismatch). `MAX_STEER_ANGLE_DEG` deliberately left unchanged — no protractor-verified angle supplied yet, only raw ADC readings. **Not yet rebuilt/reflashed.** |
| 32 | A repeated pattern (e.g. "straight line" run 3 times in a session) would have faced a different real-world direction each time, instead of the same one | `Odometry.reset()` cleared the heading reference too, not just position — `mission.py` calls `reset()` on every SWB MANUAL→AUTO rising edge, so heading re-locked to "whichever way the rover happens to be pointed" on every single mission restart, silently making the design functionally identical to "start-relative" (the option deliberately not chosen) instead of "compass-referenced" | Decoupled the two: `reset()` now only re-zeros position; the heading reference locks exactly once, on the first `update()` call ever made on an `Odometry` instance. Caught by the user asking a clarifying question about the (0,0)-on-AUTO semantics, not by the original smoke test — strengthened that test afterward to specifically catch this class of bug (call `reset()`, change the fake heading, assert the real turn is reported, not a silent re-zero). |
| 33 | Needed automatic path archival + a much richer telemetry log for plotting/tuning (cross-track error, guidance internals, full raw IMU) | Gap, not a bug — original logging only had 15 columns, no CTE metric, `--save-csv` was optional so a run's own path wasn't guaranteed recoverable | Every run now auto-saves its path to `logs/mission_<timestamp>_path.csv` (same timestamp as the telemetry log). Log expanded to 31 columns: added `cte_m`/`alpha_deg`/`lookahead_x,y_m`/`target_idx`/`mission_finished` (exposed from `guidance.PurePursuit` as `last_*` attributes) and full raw IMU (`roll/pitch/gyro_z/accel_x,y`/all 4 calibration fields/`valid`, exposed from `Odometry.last_imu_data` to avoid a redundant second I2C read per tick). First real-hardware run of `mission.py` (real IMU + STM32 link) done verifying this — which is what surfaced row 34. |
| 34 | `mission.py`'s live/logged `lookahead_x` read `10.0` (the path's FINAL waypoint) instead of `~0.8` (the lookahead distance), for a rover stationary at the path's start | `_advance_nearest_index()` advances the cursor past waypoint 0 immediately (every real mission starts with `pose == waypoints[0]`, distance 0). `_find_lookahead_point()`'s segment search started exactly AT that advanced cursor, skipping segment 0→1 — the one segment that actually intersected the lookahead circle — and fell through to its final-waypoint fallback. `guidance.py`'s smoke test started 0.5m off-axis, never triggering the cursor advance, so it never caught this | Both `_find_lookahead_point()` and `_cross_track_error()` now start their segment search one index behind the cursor (`max(0, nearest_idx-1)`) — standard handling for this known Pure Pursuit edge case. Added a smoke-test check starting exactly on waypoint 0 (the real-mission case) to catch a regression. Second bug this session found only by running on real hardware, not synthetic tests (see row 32). |
| 35 | Wanted 4 separate scripts, one per shape, prompting interactively for dimensions and saving matching-named CSVs (request arrived via a dropped file, `Documentations/test_guide.txt`, not chat) | New capability, not a bug | Added `path_prompts.py` (shared retry-on-bad-input `input()` helper) + `make_straight_path.py`/`make_rectangle_path.py`/`make_circle_path.py`/`make_lawnmower_path.py`, thin interactive wrappers over `waypoints.py` saving to `paths/<shape>_<dims>.csv`. Verified via piped stdin locally and on the real Pi — waypoint counts match the earlier `--pattern` runs exactly (11/29/64/49). |
| 36 | First real SWB=AUTO field test: rover tracked a straight line well for ~10-12m, then diverged into a growing steering oscillation ("went straight, later turning") | Two distinct causes found from log analysis: (1) `_find_lookahead_point()`'s "search one behind the cursor" logic (from row 34's fix) returned the FIRST valid intersection rather than the furthest-along one — near a waypoint boundary the behind-segment can have its own valid but backward intersection, causing single-tick `alpha` spikes to ~-170deg; (2) the linear actuator (fixed ~7mm/s, no proportional speed) chronically lagged far behind Pure Pursuit's every-50ms full-authority steering target (logged: commanded -34deg, actual measured angle only -9.7deg at the same tick) — a genuine actuator-lag-driven control instability, not a simple mistuning | Fixed (1): scan all candidate segments, keep the one with greatest cumulative path progress, not the first match — verified by replaying the exact captured glitch. Mitigated (2) as the user requested: rebuilt the speed law to actually respond to error (see row 37) — slower speed gives the lagging actuator more real time per meter traveled to catch up. **Root cause of (2) itself (the response-time mismatch) is not fully addressed** — a steering-command slew-rate limiter would target it directly; flagged to the user as an open decision, not yet built. Not yet re-tested on hardware; validated via smoke tests plus replaying the actual bad run's logged trajectory through the fixed code. |
| 37 | Speed stayed ~0.18-0.20 m/s almost constantly during the bad run above, even while heading error hit 157deg — not responsive to error as intended, and felt too fast in the field generally | Speed shaping reused `YAW_BOUND_DEG` (25deg, meant only for the STEERING law) as its own error input: `cos(25deg)=0.906`, capping the possible speed reduction at ~9% regardless of how large the real error got | Gave speed shaping its own thresholds (`SPEED_ALPHA_FULL_DEG=60`, `SPEED_CTE_FULL_M=0.8`), linear ramp down to `MIN_SPEED_FACTOR=0.3x` target as either heading or cross-track error grows (worse-of-the-two, not multiplied). Lowered `CRUISE_SPEED_MPS` 0.2->0.12. Raised `MIN_SPEED_MPS` 0.05->0.06 for margin above the STM32's brake deadband — still provisional, not confirmed against a real sustained low-speed crawl in the field. Replaying the bad run's real trajectory through the new law confirmed speed now drops to the floor exactly during the worst of the divergence. |
| 38 | Across 5 more field logs, actual wheel speed (encoder-measured) ran ~2.8-3.2x the RPi's commanded speed, consistently, worse at low commanded speeds | `_ToThrottle()` in `ackermann.c` linearly maps `(0 RPM -> THR_FWD_MIN_DAC=1100)` to `(WHEEL_RPM_MAX=20 -> DAC_MAX=4095)`. User field-measured (standing start, commanded RPM) that wheels actually overcome static friction at 3.5 RPM commanded — which the OLD formula maps to DAC=1624, not 1100. Every commanded RPM under ~3.5 was landing in a dead zone (DAC 1100-1624) producing zero real motion despite looking like a valid slow-crawl command | Raised `THR_FWD_MIN_DAC` 1100->1624 in `ackermann_config.h` (derivation in a comment there). **STM32 source change only, not yet rebuilt/reflashed.** ⚠️ Flagged as likely a PARTIAL fix: the ratio only mildly improved with higher commanded speed (3.18x at 0.06 m/s -> 2.80x at 0.12 m/s) rather than dropping toward 1.0x, suggesting the map's SLOPE (not just its floor) may also need recalibrating — pending a re-test the user offered to run. |
| 39 | Actual wheel speed ran 2.8-5.5x commanded (worse after the row-38 floor-raise, which lifted the WHOLE open-loop DAC map, not just the low end), and the root issue (map SLOPE, not just floor) can't be curve-fit reliably since terrain/traction shift it in the field anyway | `_ToThrottle()`'s open-loop RPM->DAC map is fundamentally the wrong architecture for a value that isn't fixed hardware behavior — it also varies with terrain, incline and load | Replaced with closed-loop PI (new `wheel_pid.c/h`, gains carried over from the BBB-era `ugv_pid_waypoint.py`'s proven `Kp=50, Ki=15` on the same drivetrain) for FORWARD driving only (user's explicit scope: "forward only"). `_ToThrottle()` now reverse-only; DAC is computed each tick from live encoder RPM feedback instead of a static curve. `THR_FWD_MIN_DAC` reverted 1624->1100, now just a non-critical starting floor. **Source change only, not yet rebuilt/reflashed/retested.** |
| 40 | Forward RPM ceiling capped commanded wheel speed at 20 RPM | `WHEEL_RPM_MAX` derived from `MOTOR_RPM_MAX=400`/`GEAR_RATIO=20`, but 400 RPM was the datasheet figure, not the motor's real measured max | Raised `MOTOR_RPM_MAX` 400->500 in `config.h` (field-measured true max) so `WHEEL_RPM_MAX` derives to 25.0 automatically. `GEAR_RATIO` (mechanical, fixed) untouched. **Source change only, not yet rebuilt/reflashed/retested.** |
| 41 | First PID-controlled field test: run 1 sat completely still for 27.6s at mission start (target 0.12 m/s commanded, rpm=0) before finally moving; run 2 only stalled 2.95s under identical firmware/gains | `Kp=50` alone gave too small an instant correction (DAC~1425, 35% duty) to reliably break real-world static friction, which varied hugely between runs (~90% duty needed in run 1 vs ~42% in run 2); wheel stayed still while only the PI integral slowly wound up (~23s to hit its clamp) to finally supply enough DAC | Doubled `CRUISE_SPEED_MPS` 0.12->0.24 and `MIN_SPEED_MPS` 0.06->0.12 in `rover_config.py` (RPi-side, no firmware change) — higher target RPM means a bigger instant Kp correction and faster-filling integral from the first tick, shrinking the stall. Does not fully eliminate it on high-friction ground since the integral clamp itself is unchanged; `KP_WHEEL` raise was offered but not taken this round. Cross-track error was already <7cm in both runs once moving — tracking itself confirmed working well. **Not yet retested on hardware.** |
| 42 | Row 41's "27.6s stiction stall" diagnosis was wrong | Never checked the `auto_active` telemetry column before concluding friction/integral wind-up from `rpm`/elapsed-time alone; the column reads 0 for the entire "stall" window in both runs (SWB still in MANUAL, operator hadn't flipped it yet) and `rpm_L/R` go nonzero ~150ms after it flips to 1 — a fast, normal PI response, not a slow one | User corrected directly. No code changes needed to fix this (nothing was actually broken) — `KP_WHEEL` is not implicated. The row-41 speed doubling stands on its own merit (separate "feels slow" request) but its stall-mitigation justification is retracted. Documented in scratchpad.md Mistake 16 with a general rule: check mode-gating columns (`auto_active`/`armed`/`rc_ok`/`link_ok`) before reaching for a physical explanation. |
| 43 | Straight-line runs sometimes track cleanly, sometimes show a persistent one-sided bias for the whole mission (`cte_m` never crosses zero, `steer_cmd_deg` one sign the entire run) — intermittent, present before the recent PID/speed changes too | `Odometry`'s heading reference locked from a SINGLE IMU sample at mission start; `imu_calib_mag`/`imu_calib_sys` read 0 for the entire duration of every field log collected (magnetometer never calibrates, plausibly motor/DAC interference), yet BNO055's default NDOF fusion still blends that reading into `heading_deg` — so the single lock sample is a coin-flip between close-to-true and several-degrees-off, and since the reference never re-corrects mid-mission, a bad lock steers the whole run | Lock the heading reference from the CIRCULAR MEAN of `HEADING_LOCK_SAMPLES=30` (~1.5s) consecutive readings instead of one, in `odometry.py`/`rover_config.py`. Pose held at (0,0)/invalid during the averaging window. Smoke test extended (Check 0) to verify the pose stays frozen during the window and the lock averages out injected jitter. Partial mitigation, not a full fix — a sustained interference bias present for the whole window would average through it; IMUPLUS mode or relocating the IMU are the next options if it recurs. **Not yet retested on hardware.** |
| 44 | Left/right steering pots disagree by ~1.1-1.6 deg at commanded-straight (where true Ackermann geometry says they should match) in 3 of 4 field logs, including the one run that otherwise tracked straight -- so it's a real but constant offset, not the cause of run-to-run drift variability, and worth fixing on its own | `_MapToAngle()` in `ads1115.c` trusts each wheel's `ADC_*_CENTER_RAW` as exactly 0 deg independently -- the prior pot-replacement recalibration most likely judged "straight" separately per wheel instead of against one shared physical reference for both at once | Built `calibrate_steering_pots.py`: walks left-lock/right-lock/center, live `angle_L`/`angle_R`/`delta` readout, captures via non-blocking Enter-press. Recovers true raw ADC by exactly inverting `_MapToAngle()`'s own formula using the current firmware constants (no raw-ADC field needed in the UART frame) -- round-trip verified to <1e-6 error over 1000 samples. Deployed to the RPi. **Not yet run on hardware** -- needs the user's physical walkthrough (center checked with one string-line across both wheels at once, not per-wheel) before new `ADC_L/R_MIN/CENTER/MAX_RAW` values go into `ackermann_config.h`. |
| 45 | Actuator's bang-bang-to-stop control produced ~4.3-4.5 deg of approach-direction hysteresis, and the averaged `delta` could report "centered" while the two wheels individually disagreed | No deceleration near target (abrupt full-speed stop = overshoot varying by approach direction); `_ToThrottle`-style averaging of two independent sensors can mask a real per-wheel mismatch | Activated the previously-dead `steer_pid.c`: bang-bang beyond `STEER_PROP_ZONE_DEG` (4 deg), PID inside it, floored at `ACT_MIN_DUTY_PCT` (40%, starting guess -- researched the actual actuator online, a PA-12-300-1500, no published min-duty spec exists so this still needs field verification like `THR_FWD_MIN_DAC` did). Centering (`\|target\|<CENTER_TARGET_EPS`=1 deg) now requires BOTH `angle_L`/`angle_R` individually within `STEER_DEADBAND_DEG` (narrowed 2.5 deg->1.5 deg), not just their average -- differential math (`Ackermann_ComputeRPM`) still uses the average `delta`, unchanged, per explicit user decision. Added a watchdog (`STEER_CENTER_WATCHDOG_US`=2.5s) falling back to the delta-only check if the per-wheel condition ever becomes unsatisfiable (pot drift/failing sensor), since the actuator is only rated for 10% duty cycle and indefinite hunting risks damaging it. **Not yet rebuilt/reflashed/retested** -- user will build later. |
| 46 | First flash of the bang-bang/PID actuator redesign: linear actuator wobbles/oscillates right at the zero (centered) position | `ACT_MIN_DUTY_PCT=40%` floored ANY nonzero PID correction up to 40% duty; with the tightened `STEER_DEADBAND_DEG=1.5 deg`, even a tiny correction snapped up to 40% overshot straight back out the other side of the deadband, triggering the same correction in reverse -- a self-sustaining limit-cycle, not the gentle creep-to-stop the PID zone was meant to provide | Lowered `ACT_MIN_DUTY_PCT` 40->15 in `ackermann_config.h`. Still an unverified guess, not a measured value -- if wobble persists even at 15%, next suspect is `KP_STEER`/`KI_STEER`/`KD_STEER` (also untuned BBB-ported gains, exercised on this hardware for the first time). Possible secondary contributor flagged: the new per-wheel L/R centering check could also be flip-flopping given the pot recalibration is still pending. **Not yet retested on hardware.** |
